
## 00:03 - 升级每页条数选择菜单  `frontend` `ux` `pagination`

投递记录每页条数扩展为 10、20、50、100；将原生 select 替换为胶囊按钮与浮层选项菜单，当前项带选中标记，切换时保留筛选并回到第一页。TypeScript、3 个相关测试及 Next.js 生产构建通过。

---

## 00:08 - 恢复品牌 Logo 并实现完整页码分页  `frontend` `ux` `pagination` `brand`

Header 恢复使用项目原有 /icon.svg 轨迹 Logo，移除临时 J 方块；分页控件改为克制的矩形数量菜单；应用列表查询返回筛选后的总数并支持 page 偏移分页，前端显示首尾页、当前页相邻页及省略号，可直接跳转任意展示页，同时保留上一页和下一页。TypeScript、3 个相关测试及 Next.js 生产构建通过。

---

## 00:09 - 简化跟进列表并中文化阶段分布  `frontend` `ux` `analytics`

移除需要跟进列表的展开/收起状态和按钮，全部待跟进项直接在固定高度区域内滚动；阶段分布标题 PIPELINE 改为招聘流程，未知阶段不再暴露英文枚举而显示其他阶段，现有阶段继续使用中文目录标签。TypeScript、2 个分析规则测试及 Next.js 生产构建通过。

---

## 00:12 - 将筛选分页改为客户端软导航  `frontend` `ux` `navigation`

筛选表单改用 Next.js useRouter 客户端导航，应用条件与清空不再触发浏览器整页刷新，并提供更新中状态；页码、上一页、下一页和每页数量选项统一使用 Next.js Link 且 scroll=false，服务端数据更新时保留页面框架与当前滚动位置。TypeScript、3 个相关测试及 Next.js 生产构建通过。

---

## 00:14 - 统一阶段分布字段并简化分析卡片  `analytics` `frontend` `bugfix`

定位到其他阶段源于 postgres 客户端对 JSON 对象键自动驼峰转换，而数据库 recruitment_stage 枚举正确，因此无需迁移。分析查询改为直接返回 stage 文本行并按 RECRUITMENT_STAGES 校验构造分布，类型收紧为 RecruitmentStage；删除阶段卡片右上角装饰图标，阶段列表固定高度内部滚动；移除需要跟进的 NEXT ACTION 英文。TypeScript、分析规则测试及 Next.js 生产构建通过。

---

## 00:32 - 投递状态与飞书多维表格对齐  `status` `feishu` `database` `frontend`

读取用户提供的飞书多维表格后，确认投递字段只有 Offer、已投递、拒绝。应用域、筛选表单、统计、跟进规则、导入导出与 PostgreSQL 枚举统一为 submitted、offer、refused，并已执行迁移；历史 offer/accepted 映射为 Offer，rejected/withdrawn 映射为拒绝，其余旧状态映射为已投递。数据库迁移验证、TypeScript、相关测试和生产构建通过。

---

## 00:35 - 统一自定义下拉菜单的关闭交互  `frontend` `ux` `accessibility` `dropdown`

检查全部下拉控件后，将用户菜单、导出菜单和分页每页条数菜单接入共享 DismissibleDetails；点击菜单外部、按 Escape 或选择菜单项都会收起，Escape 会恢复触发器焦点。原生 select 保持浏览器默认外部关闭行为。TypeScript、3 个组件测试和 Next.js 生产构建通过。

---

## 00:41 - 新建编辑改为页面内弹窗并重塑表单  `frontend` `ux` `dialog` `forms`

首页 Header 与空状态的新建投递改为页面内弹窗，表格编辑和详情弹窗的编辑操作改为读取完整记录后原地打开编辑表单，保存后通过 Next.js 局部刷新更新列表。统一优化输入框、选择框、文本域的底色、边框、聚焦环、占位提示和移动端布局；删除确认改为专门的危险提示卡，突出记录名称与级联删除说明。TypeScript、6 个相关组件测试及 Next.js 生产构建通过。

---

## 00:50 - 详情弹窗新增可点击招聘阶段时间线  `frontend` `ux` `timeline` `stages`

在投递详情弹窗中新增招聘阶段选择器和时间线。全部阶段可直接点击，按上海时区用今天日期调用现有阶段接口；成功后用接口返回的完整详情即时更新当前阶段、最近更新日期和时间线，并局部刷新页面统计，失败则保留原数据并显示错误。同一阶段当天已记录时禁用以避免重复。TypeScript、3 个相关组件测试与 Next.js 生产构建通过。

---

## 00:52 - 招聘阶段支持选择日期补录  `frontend` `ux` `timeline` `backfill`

阶段选择器调整为先选阶段再确认发生日期，日期默认今天，但允许选择从投递日到今天之间的历史日期，解决昨天忘记更新后次日补录的场景。同一阶段与日期组合已存在时提示并禁止重复提交；确认后即时更新时间线。TypeScript、3 个相关组件测试和 Next.js 生产构建通过。

---

## 00:57 - 已记录招聘阶段支持取消更新  `frontend` `ux` `timeline` `cancel`

澄清并实现阶段取消语义：未记录阶段点击后选择日期并新增；已记录阶段点击后展示最近一次该阶段的日期与取消确认，确认后调用现有阶段删除接口，从时间线移除该条记录并回退当前阶段。普通关闭按钮仅关闭确认区。TypeScript、4 个相关组件测试和 Next.js 生产构建通过。

---

## 01:02 - 需要跟进阈值调整为十五天  `analytics` `followup` `rules`

将已投递记录进入需要跟进的阈值从 7 天统一调整为 15 天，列表映射、分析查询与领域规则共用同一常量。边界为 14 天不进入、15 天及以上进入；Offer 和拒绝仍不进入。TypeScript、边界单元测试及 Next.js 生产构建通过。

---

## 01:06 - 优化阶段取消布局并默认简历筛选  `frontend` `ux` `database` `stages`

取消阶段确认卡改为两行布局：阶段日期与说明在上方，返回和确认取消按钮独立一行靠右，避免窄弹窗中日期和按钮换行挤压。新增数据库迁移，使所有新建投递在同一事务内自动生成投递日期当天的简历筛选阶段，版本号保持 1。迁移已实际应用；数据库验证、TypeScript、4 个组件测试和 Next.js 生产构建通过。

---

## 01:10 - 表格状态改为飞书式行内下拉  `frontend` `ux` `status` `inline-edit`

投递记录状态列由静态标签改为彩色胶囊下拉，可直接选择已投递、Offer、拒绝。切换时先读取最新详情与版本号再保存，成功后局部刷新统计与列表，失败自动恢复原状态并提示；下拉操作与整行详情点击隔离。TypeScript、3 个相关组件测试及 Next.js 生产构建通过。

---

## 01:17 - 优化投递状态下拉菜单  `frontend` `ui` `status`

将投递列表中的原生状态 select 改为彩色胶囊触发器和顶层自定义浮层，支持选中标记、点击外部或 Esc 收起，避免被表格裁切；组件测试 3 项、TypeScript 与 Next.js 生产构建均通过。

---

## 01:19 - 压缩阶段更新确认条  `frontend` `ui` `timeline`

将招聘阶段的更新确认条改为更紧凑的桌面单行布局，日期输入与按钮统一为 34px 高，并补充 720px 和 440px 的响应式排列；时间线组件测试 3 项和 TypeScript 检查通过。

---

## 01:26 - 终态进入招聘时间线  `frontend` `timeline` `status`

Offer 和拒绝通过现有状态下拉即时更新，并使用当天的 status_changed 事件作为招聘时间线末节点；状态改回已投递后终态节点从时间线撤回，不增加日期选择或确认步骤。相关组件测试 9 项、TypeScript 和生产构建通过。

---

## 01:30 - 区分需要跟进原因  `analytics` `follow-up` `frontend`

需要跟进现同时判断招聘时间线和投递记录是否满 15 天未更新，时间线停滞优先标记，并在统计卡片及投递表格明确显示对应原因和天数；Offer 与拒绝始终排除。相关测试 13 项、TypeScript 与生产构建通过。

---

## 08:21 - 优化删除确认弹框  `frontend` `ui` `dialog`

删除确认弹框移除冗余英文标题，将主标题、说明和警告区域统一左边缘，并收紧弹框宽度、间距、关闭按钮、警告卡片与操作按钮尺寸；删除弹框组件测试 2 项和 TypeScript 检查通过。

---

## 08:33 - 同步三状态契约并恢复覆盖率门禁  `specification` `testing` `authentication` `status`

依据当前实现将投递状态统一为 submitted/offer/refused，跟进阈值统一为 15 个完整日，并把规格、数据模型、OpenAPI、验收指南、README 和运维文档同步到 Better Auth 用户名认证。新增边界测试后，17 个测试文件共 42 项通过，行覆盖率 94.77%、分支覆盖率 89.88%；格式、ESLint、TypeScript 与 Next.js 16.3.0 生产构建通过。数据库、E2E、Lighthouse 和性能门禁本轮未重跑。

---
